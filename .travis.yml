language: nix
before_install:
  - nix-channel --add https://nixos.org/channels/nixos-18.09 nixpkgs
  - nix-channel --update

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  email: guilhermehas@hotmail.com
  keep_history: true
  repo: pdf-qualification
  local_dir: result
  on:
    branch: master
